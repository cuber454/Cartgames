.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store

# Собранные APK: это результат сборки, а не исходник
dist/
*.apk

# Файл автосборки живёт локально, но в репозиторий не пускается:
# у текущего токена нет права Workflows. Строку убрать, когда право появится.
.github/
